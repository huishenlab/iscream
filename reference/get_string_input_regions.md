# Return region strings from GRanges or data.frame region inputs

Return region strings from GRanges or data.frame region inputs

## Usage

``` r
get_string_input_regions(regions, feature_col = NULL)
```
