# Return the input regions as a data frame so the output can have positions as columns

Return the input regions as a data frame so the output can have
positions as columns

## Usage

``` r
get_regions_as_df(regions, regions_str)
```
